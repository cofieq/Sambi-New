import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'chefstock-pro---smart-kitchen-inventory',
  webDir: 'dist'
};

export default config;
